/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['aceternity.com','assets.aceternity.com','www.pixelstalk.net'], // Replace with your domain(s)
      },
};

export default nextConfig;
