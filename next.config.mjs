/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['aceternity.com','assets.aceternity.com'], // Replace with your domain(s)
      },
};

export default nextConfig;
