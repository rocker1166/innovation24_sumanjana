/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['aceternity.com','assets.aceternity.com','www.pixelstalk.net','www.cubesnjuliennes.com'], // Replace with your domain(s)
      },
};

export default nextConfig;
